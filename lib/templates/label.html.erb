<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="utf-8"/>
    <title>Ярлыки DPD</title>
    <style>
      @page {
        margin: 0;
      }

      body {
        width: 210mm;
        font-family: Arial;
        margin:2mm 0 0 0;
        padding:0;
      }

      div {
        overflow: hidden;
      }

      .label {
        border: 0.5mm black solid;
        position: relative;
        float: left;
        height: 55.3mm;
        width: 98mm;
        padding: 2mm 0 0 0;
      }

      .footer {
        position: absolute;
        top: 38mm;
        padding: 0 5mm 0 5mm;
        width: 85mm;
      }

      .order-number {
        margin-left: 4mm;
        text-align: left;
        font-weight: bold;
        font-size: 16pt;
        float: left;
      }

      .dpd-number {
        text-align: right;
        float: right;
        margin-right: 4mm;
      }

      .clear {
        clear: both;
      }

      .sender {
        text-align: center;
        margin: 1mm 3mm 1mm 3mm;
      }

      .receiver{
        text-align: center;
        margin: 1mm 3mm 1mm 3mm;
      }

      .barcode{
        float: left;
        text-align: center;
        font-size: 14pt;
      }

      .logo{
        text-align: center;
        float: right;
      }

      p {
        margin: 0;
        padding: 0;
      }
    </style>
  </head>
  <body>
    <% @labels.each do |label| %>
      <div class="label">
        <div class="order-number">
          <%= label.order_number %>
        </div>

        <div class="dpd-number">
          <%= label.dpd_number %>
        </div>

        <div class="clear"></div>

        <div class="sender">
          Отправитель: <%= label.sender_name %>
        </div>

        <div class="receiver">
          Получатель: <%= label.receiver_name %>
        </div>
        <div class="footer">
          <div class="barcode">
            <%= label.barcode_svg %>
            <p><%= label.barcode %></p>
          </div>
          <div class="logo">
            <img src='<%= label.logo_base_64 %>' alt='DPD'/>
          </div>
        </div>
      </div>
    <% end %>
  </body>
</html>
